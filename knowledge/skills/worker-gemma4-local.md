# Worker Skill: Gemma4 Local Reasoning

- use as the default local worker for Angella
- prefer reasoning-oriented harness tasks over coding-specific specialization
- keep context compact and focused around the benchmark contract
- use apfel only when latency is the primary objective
