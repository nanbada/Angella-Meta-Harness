# Frontier Lead Selection SOP

Use the highest available reasoning frontier model that:

1. supports the `lead` or `planner` role
2. passes credential and availability checks
3. satisfies required flags for the active profile
4. wins the selector policy ordering

Tie-break order:
- `stability_score`
- `priority`
